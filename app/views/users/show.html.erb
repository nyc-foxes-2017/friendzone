
<table class = "profile-table">
    <tr>

      <th><%=current_user.name%></th>
    </tr>
    <tr>
      <%if current_user.avatar_url %>
      <td><img src= <%=current_user.avatar_url %>/></td>
      <% end %>
    </tr>

    <tr>
      <%if current_user.friends %>
      <td><%= current_user.friends.count %> friends</td>
      <% end %>
    </tr>

  </table>

<div class="post-form">
  <%= render 'shared/post.html.erb' %>

</div>

<div class="newsfeed-center">
  <% if @posts %>
  <table class = "newsfeed-table">
    <tr>
      <% @posts.each do |post|%>
      <td><%= post.content%></td>
    </tr>
    <tr>
      <td><%= time_ago_in_words(post.created_at)%></td>

    </tr>

    <tr>
      <td><a href="#">Like</a></td>
    </tr>

  </table>
  <% else %>
  <p>There are no posts in your feed! Try making a few friend requests or post a story of your own.
  </p>
  <% end %>
</div>
